services:
  # ── Flask + Camera ─────────────────────────────
  picam:
    build: .
    container_name: picam-stream
    restart: unless-stopped
    ports:
      - "5000:5000"
    devices:
      - /dev/video0:/dev/video0
    volumes:
      - /run/udev:/run/udev:ro  # for camera detection
    environment:
      - PYTHONUNBUFFERED=1

  # ── Ngrok Tunnel ───────────────────────────────
  ngrok:
    image: ngrok/ngrok:latest
    container_name: picam-ngrok
    restart: unless-stopped
    ports:
      - "4040:4040"
    command:
      - "http"
      - "picam:5000"
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    depends_on:
      - picam
